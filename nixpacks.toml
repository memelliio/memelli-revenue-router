[phases.setup]
nixPkgs = ['nodejs_22', 'npm-10_x']

[phases.install]
cmds = ['npm install --include=dev']

[phases.build]
cmds = ['npm run build']

[start]
cmd = 'node dist/index.js'

[variables]
NODE_ENV = 'production'
